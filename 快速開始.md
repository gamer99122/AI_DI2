# 快速開始指南

## 環境需求

- **.NET 8.0 SDK**：[下載鏈接](https://dotnet.microsoft.com/download/dotnet/8.0)
- **IDE**（任選一個）：
  - Visual Studio 2022
  - Visual Studio Code
  - JetBrains Rider

## 安裝步驟

### 1. 確認 .NET 8 已安裝

```bash
dotnet --version
```

應該顯示 8.0.x 或更高版本。

### 2. 克隆或下載專案

如果從 Git 克隆：
```bash
git clone <repository-url>
cd AI_DI2
```

### 3. 開始使用

所有 .csproj 檔案已預先準備完成，無需生成。

## 執行範例

### 方式 1：使用 dotnet CLI（推薦）

```bash
# 進入範例目錄
cd Examples/Basic/Basic01_WithoutDI

# 執行
dotnet run
```

### 方式 2：使用 Visual Studio

1. 打開 `DI_Tutorial.sln`
2. 在 Solution Explorer 中右鍵點擊想要執行的專案
3. 選擇「設為啟動專案」
4. 按 F5 或點擊「開始」

### 方式 3：使用 VS Code

1. 用 VS Code 打開專案資料夾
2. 打開任意 `Program.cs` 文件
3. 按 F5 開始調試，或在終端機中運行 `dotnet run`

## 學習路徑

### 🟢 初學者（第1週）

**第1天：理解 DI 基礎**
```bash
cd Examples/Basic/Basic01_WithoutDI && dotnet run
cd Examples/Basic/Basic02_WithDI && dotnet run
```

**第2天：生命週期**
```bash
cd Examples/Basic/Basic03_TransientLifetime && dotnet run
cd Examples/Basic/Basic04_ScopedLifetime && dotnet run
cd Examples/Basic/Basic05_SingletonLifetime && dotnet run
cd Examples/Basic/Basic06_LifetimeComparison && dotnet run
```

**第3天：注入方式**
```bash
cd Examples/Basic/Basic07_InterfaceInjection && dotnet run
cd Examples/Basic/Basic08_ConstructorInjection && dotnet run
cd Examples/Basic/Basic09_MultipleServices && dotnet run
cd Examples/Basic/Basic10_ServiceResolution && dotnet run
```

**第4-5天：常用服務**
```bash
cd Examples/Basic/Basic11_ILoggerInjection && dotnet run
cd Examples/Basic/Basic12_IConfigurationInjection && dotnet run
cd Examples/Basic/Basic13_IOptionsInjection && dotnet run
```

### 🟡 中級（第2週）

**Repository 和設計模式**
```bash
cd Examples/Basic/Basic21_RepositoryPattern && dotnet run
cd Examples/Basic/Basic25_FactoryPattern && dotnet run
```

**中階技巧**
```bash
cd Examples/Intermediate/Intermediate01_ScopedFactory && dotnet run
cd Examples/Intermediate/Intermediate03_ConditionalRegistration && dotnet run
cd Examples/Intermediate/Intermediate04_ServiceLifetimeValidation && dotnet run
```

### 🔴 進階（第3週）

```bash
cd Examples/Advanced/Advanced01_CustomServiceProvider && dotnet run
cd Examples/Advanced/Advanced02_InterceptorPattern && dotnet run
cd Examples/Advanced/Advanced03_ModularArchitecture && dotnet run
cd Examples/Advanced/Advanced04_PluginSystem && dotnet run
cd Examples/Advanced/Advanced05_PerformanceOptimization && dotnet run
```

## 常見問題

### Q: 執行時出現「找不到 SDK」錯誤

**A:** 確認已安裝 .NET 8 SDK：
```bash
dotnet --list-sdks
```

### Q: 缺少 NuGet 套件

**A:** 還原套件：
```bash
dotnet restore
```

### Q: 編譯錯誤

**A:** 清理並重建：
```bash
dotnet clean
dotnet build
```

### Q: 如何建立自己的範例？

**A:** 使用模板：
```bash
dotnet new console -n MyExample
cd MyExample

# 添加 DI 套件
dotnet add package Microsoft.Extensions.DependencyInjection
dotnet add package Microsoft.Extensions.Hosting

# 開始編碼
code Program.cs
```

## 實用命令

```bash
# 列出所有可用的範例
ls Examples/Basic
ls Examples/Intermediate
ls Examples/Advanced

# 在所有範例中搜尋特定程式碼
grep -r "AddTransient" Examples/

# 執行並查看輸出
dotnet run | tee output.txt

# 清理所有編譯產物
find . -name "bin" -type d -exec rm -rf {} +
find . -name "obj" -type d -exec rm -rf {} +
```

## 除錯技巧

### 使用 Visual Studio

1. 設定中斷點（F9）
2. 啟動偵錯（F5）
3. 逐步執行（F10）
4. 查看變數值（滑鼠懸停或 Watch 窗口）

### 使用 VS Code

1. 安裝 C# extension
2. 設定 launch.json
3. 按 F5 開始偵錯

### 使用 Console 輸出

```csharp
Console.WriteLine($"服務實例 ID: {service.GetHashCode()}");
```

## 效能分析

```bash
# 使用 BenchmarkDotNet
dotnet add package BenchmarkDotNet

# 發佈優化版本
dotnet publish -c Release

# 分析記憶體使用
dotnet-counters monitor --process-id <pid>
```

## 貢獻指南

如果你想添加自己的範例：

1. Fork 專案
2. 創建新分支
3. 添加範例到對應目錄
4. 提交 Pull Request

## 更多資源

- 📖 查看 `Docs/` 目錄的詳細文檔
- 📝 查看 `範例清單.md` 了解所有範例
- 🔍 閱讀每個範例的 README.md
- 💡 實驗並修改範例程式碼

---

## 下一步

1. ✅ 完成基礎範例（Basic 01-10）
2. ✅ 閱讀文檔理解概念
3. ✅ 完成中階範例
4. ✅ 嘗試進階範例
5. ✅ 在自己的專案中應用 DI

**祝學習愉快！** 🎉
